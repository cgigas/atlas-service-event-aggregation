package atlas.event.aggregation.data.model.Satellite;

import org.junit.Test;

import static org.junit.Assert.*;

public class ApplicationTest
{
    @Test
    public void values()
    {
        assertEquals(3,Application.values().length);
    }
}